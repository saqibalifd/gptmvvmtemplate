class AppDurations {
  static const Duration second1 = Duration(seconds: 1);
  static const Duration splash = Duration(seconds: 3);
  static const Duration fade = Duration(microseconds: 500);
  static const Duration short = Duration(milliseconds: 300);
  static const Duration medium = Duration(milliseconds: 600);
  static const Duration long = Duration(milliseconds: 1200);
}
